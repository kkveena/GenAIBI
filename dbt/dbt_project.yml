name: semantic_genai_bi
version: '0.1.0'
config-version: 2

profile: semantic_genai_bi

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
